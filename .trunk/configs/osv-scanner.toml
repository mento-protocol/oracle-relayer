# osv-scanner ignore list (read by trunk's osv-scanner linter).
#
# Currently empty. Transitive advisories are patched through package.json
# "overrides" (brace-expansion per minimatch line, fast-uri, js-yaml, qs, tar,
# ...). Add an [[IgnoredVulns]] entry only when no compatible patched release
# exists, and record the reason and the consumer chain here.
